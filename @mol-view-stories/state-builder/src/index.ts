// Export UI builder types (MVS format)
export type { UINode, UIBuilderState } from './types/ui-builder';
export { createEmptyNode } from './types/ui-builder';

export const placeholder = 'state-builder';

